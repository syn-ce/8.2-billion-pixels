services:
    flask-app:
        build:
            context: ./backend
            target: production
        environment:
            - FLASK_ENV=production
        image: flask-app:prod
    nginx:
        depends_on:
            - flask-app
        build:
            context: ./one-billion-pixels-website
            target: production
        image: nginx:prod
