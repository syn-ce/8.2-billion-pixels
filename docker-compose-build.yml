services:
    flask-app:
        build:
            context: ./backend
            target: production
        environment:
            - FLASK_ENV=production
        image: flask-app:prod
    nginx:
        depends_on:
            - flask-app
        build:
            context: ./website
            target: production
        image: nginx:prod
