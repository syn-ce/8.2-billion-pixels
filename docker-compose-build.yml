services:
    #flask-app:
    #    build:
    #        context: ./backend
    #        target: production
    #    environment:
    #        - FLASK_ENV=production
    #    image: flask-app:prod
    go-server:
        build:
            context: ./backend
            target: production
        image: go-server:prod
    nginx:
        build:
            context: ./website
            target: production
        image: nginx:prod
